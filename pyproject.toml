[project]
name = "agentic-market-entry-pipeline"
version = "1.0.0"
description = "Multi-agent market-entry and fundraising diligence pipeline with sourced, citable claims."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
